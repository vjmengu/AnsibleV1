- name: TaskVariables
  hosts: local
  connection: local
  vars:
    COURSE: "Before Task Starts Var"
  tasks:
  vars:
    COURSE: " I am the 1st Var in the task"
  - name: Variables in task1
    vars:
      COURSE: " Var inside task1"
    ansible.builtin.debug:
      msg: " I AM FROM TASK 1 {{ COURSE }}" 

  - name: Variable in task2
    ansible.builtin.debug:
      msg: " I AM FROM TASK 2 {{ COURSE }}"